document.addEventListener("DOMContentLoaded", function(event) { 
  var app = new Vue({
    el: '#no',

    data: {
      games: []
    },
    mounted: function() {
    },
    methods: {
    },
    computed: {
    }
  });
});